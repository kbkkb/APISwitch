<Window x:Class="APISwitch.Dialogs.UpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource UD.Title}"
        Width="520" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BgBrush}"
        FontFamily="{StaticResource AppFontFamily}"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        Foreground="{StaticResource TextBrush}">
    <Border Padding="24" Background="{StaticResource BgBrush}">
        <StackPanel>
            <!-- Header -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="44" Height="44" CornerRadius="10" Background="#EEF2FF" BorderBrush="#C7D2FE" BorderThickness="1">
                    <Path Data="{StaticResource IconRocket}" Fill="{StaticResource AccentBrush}" Width="20" Height="20" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{DynamicResource UD.NewVersion}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                        <Border Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="10" Padding="8,2" Margin="10,0,0,0">
                            <TextBlock x:Name="VersionCompareText" Text="v0.1.1 → v0.1.2" FontSize="11" FontWeight="SemiBold" Foreground="#2563EB"/>
                        </Border>
                    </StackPanel>
                    <TextBlock x:Name="ReleaseTitleText" Text="{DynamicResource UD.ReleaseDefault}" FontSize="12" Foreground="{StaticResource TextDimBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>

            <!-- Meta Info (Published Date & Asset Size) -->
            <Border Background="{StaticResource CardBrush}" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14,10" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="PublishDateText" Text="{DynamicResource UD.PublishedFmt}" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                    <TextBlock Grid.Column="1" x:Name="PackageSizeText" Text="{DynamicResource UD.SizeFmt}" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
                </Grid>
            </Border>

            <!-- Release Notes -->
            <TextBlock Text="{DynamicResource UD.Changelog}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextDimBrush}" Margin="0,0,0,6"/>
            <Border Background="{StaticResource CardBrush}" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,16">
                <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="ReleaseNotesText" Text="{DynamicResource UD.NotesPlaceholder}" TextWrapping="Wrap" FontSize="12" Foreground="{StaticResource TextBrush}" LineHeight="18"/>
                </ScrollViewer>
            </Border>

            <!-- Download Progress Panel (Hidden by default) -->
            <StackPanel x:Name="ProgressPanel" Visibility="Collapsed" Margin="0,0,0,16">
                <Grid Margin="0,0,0,6">
                    <TextBlock x:Name="ProgressStatusText" Text="{DynamicResource UD.PreparingDownload}" FontSize="11" Foreground="{StaticResource TextDimBrush}"/>
                    <TextBlock x:Name="ProgressPercentText" Text="0%" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Right"/>
                </Grid>
                <ProgressBar x:Name="DownloadProgressBar" Height="8" Minimum="0" Maximum="100" Value="0" Background="#E2E8F0" Foreground="{StaticResource AccentBrush}"/>
            </StackPanel>

            <!-- Action Buttons -->
            <Grid Margin="0,4,0,0">
                <Button Content="{DynamicResource UD.GitHubBtn}" Style="{StaticResource GhostButton}" Click="OnOpenGitHub" HorizontalAlignment="Left" Padding="12,7">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource IconExternal}" Fill="{StaticResource TextDimBrush}" Width="12" Height="12" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="{DynamicResource UD.GitHubBtn}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnCancel" Content="{DynamicResource UD.LaterBtn}" Click="OnCancel" Margin="0,0,10,0" Padding="14,7" Style="{StaticResource GhostButton}"/>
                    <Button x:Name="BtnInstall" Content="{DynamicResource UD.GoGitHubDownload}" Style="{StaticResource PrimaryButton}" Click="OnInstall" Padding="16,7" FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Border>
</Window>