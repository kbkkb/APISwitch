<Window x:Class="APISwitch.Dialogs.CopyTargetsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource CD.Title}"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BgBrush}"
        FontFamily="{StaticResource AppFontFamily}"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        Foreground="{StaticResource TextBrush}">
    <StackPanel Margin="22">
        <TextBlock x:Name="SourceText" Text="{DynamicResource CD.SourceDefault}" FontSize="13"
                   Foreground="{StaticResource TextDimBrush}" Margin="0,0,0,12"/>
        <CheckBox x:Name="ClaudeCheck" Content="Claude Code (CLI)" Margin="0,0,0,8"/>
        <CheckBox x:Name="DesktopCheck" Content="Claude Desktop" Margin="0,0,0,8"/>
        <CheckBox x:Name="CodexCheck" Content="Codex" Margin="0,0,0,8"/>
        <CheckBox x:Name="OpenCodeCheck" Content="OpenCode" Margin="0,0,0,8"/>
        <CheckBox x:Name="PiCheck" Content="Pi" Margin="0,0,0,4"/>
        <TextBlock Text="{DynamicResource CD.MappingHint}"
                   FontSize="11" Foreground="{StaticResource TextDimBrush}" TextWrapping="Wrap" Margin="0,10,0,0"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="{DynamicResource PD.CancelBtn}" Click="OnCancel" Margin="0,0,10,0" IsCancel="True"/>
            <Button Content="{DynamicResource Common.Copy}" Style="{StaticResource PrimaryButton}" Click="OnOk" IsDefault="True" MinWidth="90"/>
        </StackPanel>
    </StackPanel>
</Window>
